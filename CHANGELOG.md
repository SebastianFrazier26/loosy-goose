# Changelog

## Unreleased

- 2026-09-08 — project scaffold: uv project, `spectral` core seed (PPMI, IPR, truncated SVD), CLI stub, CI, docs.
- 2026-09-08 — Phase 1 foundation: `transcript` loaders (Claude Code JSONL, generic messages), `segment` with protected units and atom extraction, `tokens` (tiktoken o200k), corpus scripts (`fetch_public`, `pick_local`, `corpus_stats`), `.gitattributes` LF normalisation, optional `embed` extra installed locally.
- 2026-09-08 — Phase 1 experiments: shared token-budget contract (`budget`), distortion metrics (`metrics`, `embed`), baselines; Experiment A (`cooccur`, `topics`), Experiment B (`select`), Experiment C (`code`, `supersede`).
- 2026-09-09 — Phase 1 documented: `docs/DESIGN.md` (algorithm spec, per-kind quality table replacing the binary `protected` flag), `docs/PHASE1.md` (designs, pre-registered criteria, results, known defects), `docs/results/` (committed summary tables). README and CLAUDE.md corrected — they still described the superseded protect-verbatim design.
- 2026-09-09 — segmenter and tokenizer defect fixes ahead of Phase 2: drop punctuation-only prose segments (137 to 0 across the corpus), bound prose and tool_result segments that carry no sentence or line breaks (largest prose segment 7,602 to 347 tokens), and neutralise JSON escape sequences in the co-occurrence tokenizer (27,650 corrupted tokens to 1).
- 2026-09-09 — `metrics.atom_recall_final`: atom recall against the session's final-state atom set (`apply_supersession(original)`), reported alongside the existing all-history `atom_recall` rather than replacing it — the latter counts atoms from superseded reads/edits that no longer describe anything real, which `atom_recall_final` does not.
- 2026-09-09 — Phase 2 rate-distortion runner (`experiments/exp_d_curves.py`): every method (baselines, Experiment B strategies, Experiment A as labeller, Experiment C stacked supersede/band variants) on one curve per transcript at a shared token budget, checkpointed per transcript. Plots and the cross-method head-to-head are keyed on ACHIEVED compression ratio, not requested keep_ratio, since a supersede-based method's candidate pool can be smaller than its budget (`budget_binding` on each record; per-transcript ceiling surfaced in the summary).

# Data

- `data/local/` — gitignored. Holds the user's own LLM session logs used as a local development
  corpus. Nothing under it is ever committed.
- `data/public/` — public dialogue fixtures and synthetic sessions used by tests and experiments.
